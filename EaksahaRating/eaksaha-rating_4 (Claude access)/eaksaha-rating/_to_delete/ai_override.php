<?php
// ============================================================
//  เลิกใช้แล้ว — ระบบไม่มีการแก้คะแนนอีกต่อไป
// ------------------------------------------------------------
//  คะแนนทุกรายการมาจาก AI อ่านสิ่งที่ลูกค้าเขียนล้วนๆ ไม่มีใครแก้ทับได้
//  ถ้าเห็นว่าเคสไหนควรคุยกับพนักงาน ให้ใช้ปุ่ม "ส่งข้อความ"
//  ในหน้ารายการรีวิวแทน
//
//  ไฟล์นี้เหลือไว้เฉยๆ เพื่อกันหน้าเก่าที่ยังค้างอยู่ในเบราว์เซอร์ยิงเข้ามา
//  ลบทิ้งได้เลยถ้าไม่มีใครเปิดค้างไว้แล้ว
// ============================================================

header('Content-Type: application/json; charset=utf-8');
http_response_code(410);   // 410 Gone = เคยมี แต่เอาออกถาวรแล้ว
echo json_encode([
    'ok'    => false,
    'error' => 'ระบบยกเลิกการแก้คะแนนแล้ว — ใช้ปุ่ม "ส่งข้อความ" เพื่อแจ้งพนักงานแทน',
], JSON_UNESCAPED_UNICODE);
