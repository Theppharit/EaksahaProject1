# ============================================================
# package ที่โปรเจกต์ต้องใช้
# ติดตั้ง:  pip install -r requirements.txt
# ============================================================

# ── หลัก ────────────────────────────────────────
Django>=5.2,<6.0            # LTS ซัพพอร์ตถึง เม.ย. 2028

# ── ตั้งค่าผ่านไฟล์ .env ─────────────────────────
django-environ>=0.11,<1.0

# ── จำเป็นสำหรับ ImageField (รูปถ่ายรถ · รูปคนขับ) ──
Pillow>=11.0

# ── ใช้ตอน deploy ───────────────────────────────
gunicorn>=23.0              # ตัวรันจริงแทน runserver (Linux เท่านั้น)
whitenoise[brotli]>=6.7     # ให้ Django เสิร์ฟ static ได้เอง ไม่ต้องตั้ง nginx เพิ่ม
psycopg[binary]>=3.2        # ตัวเชื่อม PostgreSQL

# หมายเหตุ: gunicorn ติดตั้งบน Windows ไม่ได้ (ใช้ได้เฉพาะ Linux)
# ถ้า pip install แล้วติดที่บรรทัดนี้ ให้ใช้ requirements-dev.txt แทน
