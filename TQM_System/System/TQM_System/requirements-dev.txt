# ============================================================
# package สำหรับเครื่องพัฒนา (Windows)
# ติดตั้ง:  pip install -r requirements-dev.txt
# ------------------------------------------------------------
# ตัดตัวที่ลงบน Windows ไม่ได้ออก (gunicorn) และตัวที่ยังไม่ต้องใช้
# ============================================================

Django>=5.2,<6.0
django-environ>=0.11,<1.0
whitenoise[brotli]>=6.7

# จำเป็นสำหรับ ImageField (รูปถ่ายรถ · รูปคนขับ) — ถ้าไม่มี migrate จะไม่ผ่าน
Pillow>=11.0

# เปิดใช้เมื่อย้ายไป PostgreSQL บนเครื่องพัฒนาด้วย
# psycopg[binary]>=3.2

# ── เครื่องมือช่วยพัฒนา (ไม่บังคับ แต่แนะนำมาก) ──
# แสดงจำนวน SQL query ของแต่ละหน้าที่มุมจอ — จับปัญหา N+1 ได้ตั้งแต่เนิ่น ๆ
django-debug-toolbar>=4.4
