from django.apps import AppConfig


class CoreConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "project.core"
    label = "core"
    verbose_name = "ของกลาง — แจ้งเตือน · เอกสาร"
